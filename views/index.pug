extends layout

block content
  .main.container.text-xs-center
    h1.display-3 Good Food App
    p.lead Join us to find convenient food delivery services!
    a.btn.btn-info-outline.btn-lg(href='/register') Register
